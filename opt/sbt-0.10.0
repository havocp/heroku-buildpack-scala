#!/usr/bin/env bash

BIN_DIR=$(cd $(dirname $0); pwd) # absolute path
mkdir -p "$BIN_DIR"/boot
/usr/bin/java $SBT_OPTS -Dsbt.boot.directory="$BIN_DIR"/boot -jar `dirname $0`/sbt-launch-0.10.0.jar "$@"
